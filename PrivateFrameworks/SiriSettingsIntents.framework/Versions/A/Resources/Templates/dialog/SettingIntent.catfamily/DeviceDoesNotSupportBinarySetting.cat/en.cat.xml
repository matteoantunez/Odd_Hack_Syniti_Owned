<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<cat xmlns="urn:apple:names:siri:cat:1.0" id="SettingIntent#DeviceDoesNotSupportBinarySetting" locale="en">
    <first>
        <dialog condition="deviceState.isPhone"><var name="setting.name"/> isn't available on this iPhone.</dialog>
        <dialog condition="deviceState.isPad"><var name="setting.name"/> isn't available on this iPad.</dialog>
        <dialog condition="deviceState.isWatch"><var name="setting.name"/> isn't available on this Apple Watch.</dialog>
        <dialog condition="deviceState.isAppleTV"><var name="setting.name"/> isn't available on this Apple TV.</dialog>
        <dialog condition="deviceState.isMac"><var name="setting.name"/> isn't available on this Mac.</dialog>
        <dialog condition="deviceState.isSidekick"><var name="setting.name"/> isn't available on this device.</dialog>
        <dialog condition="deviceState.isHomePod"><var name="setting.name"/> isn't available on this HomePod.</dialog>
        <dialog><var name="setting.name"/> isn't available on this device.</dialog>
    </first>
</cat>
